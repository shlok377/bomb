# Stage 1: Project Setup & Landing Page

## Objectives
- Establish the base project structure and web server (if any) or static file architecture.
- Build the initial Landing Screen.
- Implement the "Expert" and "Defuser" navigational flows.

## Tasks
1. Initialize basic HTML, CSS, and JS structure.
2. Create `index.html` (Landing Page).
3. Implement the "Read the Manual" flow:
   - Create an embedded `iframe` overlay for `pdf/manual.pdf`.
   - Add a "Back to Main Menu" button.
4. Implement the "Defuse the Bomb" flow:
   - Build the Phase/Level selection popup/modal.
   - Wire up navigation so clicking a level redirects to a phase-specific HTML file (e.g., `phase1.html`).
